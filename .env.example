# .env is loaded into the process environment at startup so that
# config.json can reference any variable here via $VAR or ${VAR}.
#
# All proxy settings (host/port/upstream/backend wiring) live in
# config.json — this file is purely for secrets.

# MINIMAX_API_KEY=sk-...
